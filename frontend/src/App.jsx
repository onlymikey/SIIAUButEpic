import LoginCard from "./components/LoginCard";

export default function App() {
  return (
    <main className="dark text-foreground bg-background w-full h-screen flex items-center justify-center">
      <LoginCard />
    </main>
  )
}